package = "SnabbP4"
version = "0.0.1-1"
source = {
	url = "..." -- We don't have one yet
}
description = {
	summary = "p4 parser and (eventually) runtime.",
	detailed = [[
		 This is an example for the LuaRocks tutorial.
		 Here we would put a detailed, typically
		 paragraph-long description.
	]],
	homepage = "https://ericbullington.com"
}
dependencies = {
	"lpeg ~> 1.0.0-1"
	"busted ~> 2.0"
	"luacheck ~> 0.13.0-1"
}
build = {
	-- We'll start here.
}
